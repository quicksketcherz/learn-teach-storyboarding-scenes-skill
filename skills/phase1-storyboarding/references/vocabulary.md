# Vocabulary

The runner's compositional dialect — words and phrases they actually reach for when reading a frame. Loaded at Step 5 (Annotate) so the agent comments back using *their* words, not generic composition vocabulary. Grows over runs via propose-and-approve at close-out.
